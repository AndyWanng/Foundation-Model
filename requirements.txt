# One-file CUDA 12.8 server environment. These versions are pinned for the
# server pipeline; bootstrap verifies them with pip check but never starts
# preprocessing, a CUDA forward/backward pass, or the formal experiment.
# MONAI is deliberately absent because this implementation does not use it.
--index-url https://pypi.org/simple
--extra-index-url https://download.pytorch.org/whl/cu128

torch==2.11.0+cu128
torchvision==0.26.0+cu128
numpy==2.2.6
scipy==1.15.3
PyYAML==6.0.3
nibabel==5.3.2
indexed-gzip==1.10.1
psutil==7.1.3
einops==0.8.2
timm==1.0.28
filelock==3.20.0
rich==14.1.0
pydicom==3.0.1
SimpleITK==2.5.2
pytest==9.0.3
